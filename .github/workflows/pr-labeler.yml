name: PR Labels

# Use pull_request_target so the workflow runs in the base repo's context with a
# read-write token — pull_request grants only a read-only token to PRs from
# forks, which makes labeling fail with "Resource not accessible by integration"
# on every external contribution. This is safe here because the job only reads PR
# metadata and applies labels; it never checks out or executes the PR's code.
on:
  pull_request_target:
    types: [opened, synchronize]

permissions:
  contents: read
  pull-requests: write

jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/labeler@v6
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Label PR size
        uses: actions/github-script@v9
        with:
          script: |
            const { data: pr } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number,
            });
            const lines = pr.additions + pr.deletions;
            const sizeLabels = { 'size: XS': 10, 'size: S': 50, 'size: M': 200, 'size: L': 500 };
            let label = 'size: XL';
            for (const [name, max] of Object.entries(sizeLabels)) {
              if (lines < max) { label = name; break; }
            }
            // Remove existing size labels
            const existing = (await github.rest.issues.listLabelsOnIssue({
              owner: context.repo.owner, repo: context.repo.repo, issue_number: context.issue.number
            })).data.map(l => l.name).filter(n => n.startsWith('size:'));
            for (const old of existing) {
              if (old !== label) await github.rest.issues.removeLabel({ owner: context.repo.owner, repo: context.repo.repo, issue_number: context.issue.number, name: old }).catch(() => {});
            }
            await github.rest.issues.addLabels({ owner: context.repo.owner, repo: context.repo.repo, issue_number: context.issue.number, labels: [label] });
